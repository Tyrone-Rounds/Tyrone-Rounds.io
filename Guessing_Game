<!DOCTYPE html>
<html>
<head>
	<title>Add Text</title>
</head>
<body>
	<form>
		Enter a number here:

		<input type="text" id="newNumber">
		<button type = "button" onclick = "outputText()">Click Me</button>
	</form>
	<p id = "output"></p>
	<p id = "loserCount"></p>

 <script>
 	var theTarget = Math.ceil(Math.random()*100);
 	
 	function outputText(){
 		var resultant = document.getElementById("output");
 		var guess = document.getElementById("newNumber").value;
 		var numGuesses = 1;

 		if (numGuesses != 10){
 			

 			if(guess < theTarget){
 				resultant.innerHTML = resultant.innerHTML + "<li>" + guess + " is too low.</li>";
 				numGuesses = numGuesses + 1;

 			} else if(guess > theTarget){
 				resultant.innerHTML = resultant.innerHTML + "<li>" + guess + " is too high.</li>"
 				numGuesses = numGuesses + 1;
 			} else {
 				resultant.innerHTML = resultant.innerHTML + "<li>" + guess + " is correct <h1>YOU WIN!!</h1></li>";
 			numGuesses = numGuesses + 1;
 			}
 		} else {
 			resultant.innerHTML = resultant.innerHTML + "<li>" + guess + " is the maximum number of Guesses <h1>YOU LOSE!!</h1></li>"
 }
 }//Useless text
 
 </script>
</body>
</html>
